%h1= @user.name

Edit account.  Your name may not be changed.

- form_for @user, :url => account_url do |f|
  = f.error_messages
  = render :partial => "form", :object => f
  /- if not @user.roles.name_is('admin').empty?
  /- if Role.includes(:users).where(name: 'admin').where('users.id' => @user.id).exists?
  - if is_admin?
    = collection_select :user, :role_ids,                |
      Role.all(:order => 'name ASC'), :id, :name,        |
      { :selected => @user.role_ids },                   |
      { :multiple => true, :name => 'user[role_ids][]' } |
  %p
    = f.submit "Update", class: :button
-# = link_to "My profile", account_path, class: :button
